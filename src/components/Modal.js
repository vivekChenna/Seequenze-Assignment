import React from "react";

const Modal = () => {
  return (
    <div className=" fixed inset-0 bg-orange-100 bg-opacity-30 backdrop-blur-sm">
      hello i am a modal
    </div>
  );
};

export default Modal;
